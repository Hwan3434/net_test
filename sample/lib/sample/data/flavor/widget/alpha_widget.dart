import 'package:flutter/material.dart';

class AlphaWidget extends StatelessWidget {
  final Widget child;
  const AlphaWidget({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
