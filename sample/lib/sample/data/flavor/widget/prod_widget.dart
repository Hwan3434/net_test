
import 'package:flutter/material.dart';

class ProdWidget extends StatelessWidget {
  final Widget child;
  const ProdWidget({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
